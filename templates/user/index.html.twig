{% extends 'base.html.twig' %}

{% block title %}All Missions{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    <div >
        <h1 class="ml-4"    >All users </h1>
        <p class="ml-4" >Today {{ pluralize(users|length, 'Heroe', 'Heroes') }} </p>
        <br>

        <br>

        <div class="row">


            {% for user in users %}
                <div class="card shadow mb-4 ml-5  col-lg-5">
                    <div class="card-header py-8">
                        <h6 class="m-0 font-weight-bold text-primary">{{ user.username }}</h6>
                    </div>
                    <div class="card-body">
                        <img src="{{ user.image }}" alt="">
                        <p>test</p>
                        <a href="{{path('app_heroes_show', {'id': user.id}) }}" class="btn btn-primary btn-icon-split">
                                        <span class="icon text-white-50">
                                            <i class="fas fa-arrow-right"></i>
                                        </span>
                            <span class="text">See the Heroe</span>
                        </a>
                      {#   <a href="{{ path('app_user_show', {id: user.id}) }}" class="btn btn-primary btn-icon-split">
                                        <span class="icon text-white-50">
                                            <i class="fas fa-arrow-right"></i>
                                        </span>
                            <span class="text">See the user</span>
                        </a> #}

                    </div>
                    <div class="card-footer">
                        <p>test</p>
                    </div>
                </div>

            {% endfor %}
        </div>
    </div>
{% endblock %}
